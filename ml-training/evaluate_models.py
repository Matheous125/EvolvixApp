"""
evaluate_models.py — Produce thesis metrics tables + plots for all 3 models.

Implementation belongs to PLAN.md §6.5.5. This stub exists so the folder
structure required by §6.5.1 is complete.

Planned outputs (under ml-training/data/plots/):
  - confusion matrices (PNG) for all three models
  - ROC curve (Model 1)
  - calibration plot (Model 1)
  - classification reports (Models 2 & 3)
  - aggregated Markdown metrics table (stdout) for the thesis ML chapter
"""

if __name__ == "__main__":
    raise NotImplementedError("Implemented in PLAN.md §6.5.5.")
